version: 2

sources:
  - name: raw          # the name used to reference this source group in {{ source('raw', 'table') }}
    schema: raw          # the actual schema name in the database
    tables:
      - name: users
      - name: events
      - name: transactions
      - name: experiment_assignments
